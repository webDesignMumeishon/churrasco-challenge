/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // images: {
  //   domains: ["localhost:4000"],
  //   loader: 'customer',
  //   path: '/'
  // }
}

module.exports = nextConfig
